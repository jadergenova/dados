services:
  - type: web
    name: supermarket-analytics-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false  # Preencher com a URL do backend após o deploy
